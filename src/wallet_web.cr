# `WalletWeb` keeps the controllers and views which
# define your web interface.
#
# `WalletWeb` is also responsible for housing the middleware
# and back-end logic related content.
module WalletWeb; end
